# Task 9 — Atualizar README/docker-compose/secrets

**Depende de:** Task 8
**Seção da SPEC:** —
**Modelo sugerido:** Sonnet 5 (thinking-high)

## Prompt (copie tudo abaixo para uma nova sessão de chat)

```
Repositório: servidor MCP n8n com a Fase 1 da SPEC implementada (Tasks 0-8).

Tarefa:
1. Atualizar README.md: tabela de "Ferramentas disponíveis" com todas as tools novas
   e otimizadas; atualizar a seção "Simplicidade vs. completude" para refletir o que
   foi de fato implementado (não mais "planejado").
2. Se ALLOW_DEFAULT_N8N_CREDENTIALS (Task 8) ou N8N_SSRF_MODE foram adicionados,
   documentar essas envs no README e, se aplicável, no docker-compose.yml /
   exemplo de secrets.
3. Confirmar que package.json tem os scripts start/dev/test corretos (seção 10.3 da SPEC).
4. Atualizar .context/onboarding/AI_CONTEXT.md e .context/spec/TECHNICAL_SPEC_COMPACT.md
   para refletir a arquitetura final implementada (não mais "planejada") e adicionar
   uma entrada no changelog de .context/onboarding/AI_CONTEXT.md com a data de hoje.

Restrições: não altere comportamento de código nesta task, só documentação/config.

Reporte: arquivos atualizados e diff resumido das principais mudanças no README.
```
